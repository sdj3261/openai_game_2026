# 8초 도둑단 디자인 규칙

목표는 장난감 게임처럼 **보면 알고, 누르면 바로 반응하는 것**입니다. 특정 고전 게임의 캐릭터나 블록을 복제하지 않고, 시계태엽 장난감 도시만의 실루엣을 유지합니다.

## SIMPLE IS BEST

1. 규칙은 세 문장입니다: `8초 안에 열쇠 찾기 → 출구 열기 → 탈출`.
2. 조작은 이동, `Z 경비원 도발`, `X 분신 만들기` 세 가지뿐입니다.
3. 메뉴에는 `스테이지 이름`, `한 줄 내용`, `현재 목표`, `난이도`, `최고 기록`, `시작`만 표시합니다. 긴 브리핑과 음악 정보는 첫 화면에 두지 않습니다.
4. 한글 제목과 버튼은 작은 크기에서도 읽히는 게임 글꼴, 긴 설명은 시스템 UI 글꼴을 사용합니다. 다른 언어의 이름을 한 화면에 병기하지 않습니다.
5. 장소색이 바뀌어도 의미색은 고정합니다: 플레이어 파랑, 분신별 고유색, 열쇠 노랑, 출구 초록, 경비·레이더 빨강.
6. 상태는 색만으로 전달하지 않습니다. `현재`, `다음`, `잠김`, 경비 실루엣, `!`, `보스·Z`, 소리 자막을 함께 표시합니다.

## 화면 부품

- `.topbar`: 현재 스테이지, 차례, 분신, 남은 시간 표시
- `.toy-stage-card`: 현재 스테이지의 한 줄 내용·목표·난이도·최고 기록·시작
- `.toy-rail`: 8개 스테이지의 완료·현재·다음·잠김 상태
- `.mobile-gamepad`: 조이스틱과 `Z`/`X` 큰 버튼
- `.sound-caption`: 소리·문·열쇠·경비·보스 이벤트와 방향 표시
- `.profile-dialog`, `.records-dialog`: 캐릭터 꾸미기, 통계와 기기 랭킹

메뉴의 채운 주요 버튼은 `시작` 하나뿐입니다. 프로필·기록·설정은 보조 버튼으로 분리하고, 세부 통계와 언어·접근성 설정은 필요한 순간에만 별도 창으로 보여줍니다. 모든 터치 버튼은 최소 48px입니다. 세로 화면은 플레이어 추적 카메라, 짧은 가로 화면은 게임과 조작기를 나란히 사용합니다. 메뉴가 열려 있을 때 게임 조작기는 숨깁니다.

## 스프라이트 제작

캐릭터 원본은 내장 이미지 생성으로 만들고, Agent Sprite Forge의 `generate2dsprite` 프로세서로 마젠타 제거·프레임 분할·정렬·QC를 수행합니다. 현재 메뉴의 지금이와 최종 보스는 이 파이프라인의 투명 GIF를 사용합니다. 원본 시트, 프레임, GIF, 프롬프트, `pipeline-meta.json`을 함께 보관해 재현성과 검수 근거를 남깁니다. 특정 상용 게임의 캐릭터·복장·블록은 복제하지 않고, 큰 실루엣과 제한된 색처럼 일반적인 판독 원칙만 참고합니다.

## 판정 규칙

빨간 레이더와 실제 발각 판정은 같은 시야 폴리곤을 사용합니다. 광선은 벽과 닫힌 문에서 멈추며, 플레이어 원이 이 폴리곤에 닿을 때만 발각 게이지가 오릅니다. 몸이 직접 닿으면 즉시 잡힙니다. `Z 경비원 도발`은 공격이 아니라 현재 위치로 경비원을 불러오는 선택 행동입니다.

## 난이도와 음악

모든 곡은 정확히 8초이며 새 차례와 함께 첫 박으로 돌아갑니다. 음량보다 BPM·음표 밀도·경비 수로 난이도를 표현합니다.

| STAGE | 장소 | 경비 | 권장 분신 | BGM |
|---:|---|---:|---:|---:|
| 01 | 태엽 박물관 | 1 | 0 | 90 BPM |
| 02 | 장난감 공장 | 1 | 0 | 105 BPM |
| 03 | 캔디 아케이드 | 2 | 1 | 120 BPM |
| 04 | 구름 연구실 | 2 | 2 | 135 BPM |
| 05 | 달빛 역 | 3 | 1 | 150 BPM |
| 06 | 거울 성 | 3 | 2 | 165 BPM |
| 07 | 왕실 금고 | 4 | 2 | 180 BPM |
| 08 | 자정 시계탑 | 4 | 2 | 195 BPM 보스 패턴 |

딱걸이는 보라색 큰 몸, 시계 고리, 붉은 단안, `보스·Z` 표기로 구분합니다. 쓰러뜨리는 적이 아니라 `Z 경비원 도발`로 다른 곳에 불러 통과하는 최종 잠입 장애물입니다.

분신은 `X`를 직접 눌렀을 때만 만들어집니다. 발각과 `R` 재시작은 재시도를 늘리지만 분신을 만들지 않습니다. 8초가 끝나면 화면이 멈추고, `X`로 분신을 만들거나 `R`로 기록을 버릴 수 있습니다. 탈출 조건은 열쇠 하나뿐입니다. 열쇠를 얻으면 출구가 열리고, 분신과 `Z`는 해결을 돕는 선택 도구입니다. 분신은 최대 10개이며 권장 수를 넘기면 잠입 점수가 내려갑니다.

## 저장과 접근성

프로필, 설정, 통계, 기록은 브라우저 `localStorage`에 저장합니다. 현재 랭킹은 같은 기기의 기록 비교이며, 서버형 전체 랭킹은 HIVE 연동 단계에서 확장합니다. 언어는 한국어·영어·일본어를 지원합니다. 소리 표시를 켜면 효과음 자막·방향 화살표·HUD 박자가 제공됩니다.

## 새 스테이지 추가

`game.js`의 `THEMES`와 `levels`, `i18n.js`의 세 언어 문구를 함께 추가합니다. 음악 길이가 8초가 아니면 시작 시 오류가 발생합니다. 새 경비도 화면 실루엣, 레이더 판정, 소리 자막을 동시에 검수합니다.
