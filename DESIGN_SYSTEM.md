# 8초 도둑단 디자인 규칙

목표는 장난감 게임처럼 **보면 알고, 누르면 바로 반응하는 것**입니다. 특정 고전 게임의 캐릭터나 블록을 복제하지 않고, 시계태엽 장난감 도시만의 실루엣을 유지합니다.

## SIMPLE IS BEST

1. 규칙은 세 문장입니다: `8초 움직여 → X로 복제 → 보석 들고 탈출`.
2. 조작은 이동, `Z 소음`, `X 복제` 세 가지뿐입니다.
3. 메뉴에는 현재 목표와 다음 스테이지만 크게 표시합니다. 긴 브리핑은 만들지 않습니다.
4. 한글·일본어는 읽기 쉬운 UI 글꼴, 영문 장식 글자만 둥근 display 글꼴을 사용합니다.
5. 장소색이 바뀌어도 의미색은 고정합니다: 플레이어 파랑, 아까미 청록, 보석 노랑, 출구 초록, 경비·레이더 빨강.
6. 상태는 색만으로 전달하지 않습니다. `현재`, `다음`, `잠김`, 경비 실루엣, `!`, `BOSS·Z`, 소리 자막을 함께 표시합니다.

## 화면 부품

- `.topbar`: 현재 스테이지, 루프, 아까미, 남은 시간, 4박 시각 표시
- `.core-loop-strip`: 핵심 규칙 3단계
- `.stage-diorama`: 지금이·아까미·경비·레이더·보석·출구 미리보기
- `.toy-stage-card`: 현재 목표, 레이더 규칙, 다음 스테이지, 난이도·경비·추천 아까미
- `.toy-rail`: 8개 스테이지의 완료·현재·다음·잠김 상태
- `.mobile-gamepad`: 조이스틱과 `Z`/`X` 큰 버튼
- `.sound-caption`: 소음·문·보석·경비·보스 이벤트와 방향 표시
- `.profile-dialog`, `.records-dialog`: 캐릭터 꾸미기, 통계와 기기 랭킹

모든 터치 버튼은 최소 44px입니다. 세로 화면은 플레이어 추적 카메라, 짧은 가로 화면은 게임과 조작기를 나란히 사용합니다. 메뉴가 열려 있을 때 게임 조작기는 숨깁니다.

## 판정 규칙

빨간 레이더와 실제 발각 판정은 같은 시야 폴리곤을 사용합니다. 광선은 벽과 닫힌 문에서 멈추며, 플레이어 원이 이 폴리곤에 닿을 때만 발각 게이지가 오릅니다. 몸이 직접 닿으면 즉시 잡힙니다. `Z`는 공격이 아니라 소음 미끼입니다.

## 난이도와 음악

모든 곡은 정확히 8초이며 루프와 함께 첫 박으로 돌아갑니다. 음량보다 BPM·음표 밀도·경비 수로 난이도를 표현합니다.

| STAGE | 장소 | 경비 | 추천 아까미 | BGM |
|---:|---|---:|---:|---:|
| 01 | 태엽 박물관 | 1 | 0 | 90 BPM |
| 02 | 장난감 공장 | 1 | 1 | 105 BPM |
| 03 | 캔디 아케이드 | 2 | 2 | 120 BPM |
| 04 | 구름 연구실 | 3 | 2 | 135 BPM |
| 05 | 달빛 역 | 3 | 2 | 150 BPM |
| 06 | 거울 성 | 3 | 2 | 165 BPM |
| 07 | 왕실 금고 | 4 | 3 | 180 BPM |
| 08 | 자정 시계탑 | 4 | 3 | 195 BPM 보스 패턴 |

딱걸이는 보라색 큰 몸, 시계 고리, 붉은 단안, `BOSS·Z` 표기로 구분합니다. 쓰러뜨리는 적이 아니라 소리로 유인해 통과하는 최종 잠입 장애물입니다.

## 저장과 접근성

프로필, 설정, 통계, 기록은 브라우저 `localStorage`에 저장합니다. 현재 랭킹은 같은 기기의 기록 비교이며, 서버형 전체 랭킹은 HIVE 연동 단계에서 확장합니다. 언어는 한국어·영어·일본어를 지원합니다. 소리 표시를 켜면 효과음 자막·방향 화살표·HUD 박자가 제공됩니다.

## 새 스테이지 추가

`game.js`의 `THEMES`와 `levels`, `i18n.js`의 세 언어 문구를 함께 추가합니다. 음악 길이가 8초가 아니면 시작 시 오류가 발생합니다. 새 경비도 화면 실루엣, 레이더 판정, 소리 자막을 동시에 검수합니다.
